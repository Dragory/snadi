{
  "name": "@snadi/kysely",
  "license": "MIT",
  "version": "0.0.3",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test dist",
    "publish": "npm run build && npm run test && npm publish --auth-type=legacy --access public"
  },
  "peerDependencies": {
    "@snadi/core": "*",
    "kysely": "*"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.4",
    "better-sqlite3": "^8.5.1"
  }
}
